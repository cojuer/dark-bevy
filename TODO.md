# TODO

## Deferred

- Validate the Linux target locally, including headless workspace builds and
  tests.
- Add a Linux job to GitHub Actions for formatting, Clippy, tests, and workspace
  builds.
- Add `cargo-deny` to GitHub Actions to check dependency advisories, licenses,
  bans, and sources using `deny.toml`.
